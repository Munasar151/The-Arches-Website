/*This function from the html and this will enable the user to change the backgrounf colour once the button is clicked.*/
		
function backgrounddark() 
{
    var element = document.body;
    element.classList.toggle("darkmode");
}
